[DEFAULT]
ignoreip = {{ security_f2b_ignoreip | join(' ') }}
bantime  = {{ security_f2b_bantime }}
findtime = {{ security_f2b_findtime }}
maxretry = {{ security_f2b_maxretry }}

backend = systemd

[sshd]
enabled = true
port    = ssh
logpath = %(sshd_log)s
